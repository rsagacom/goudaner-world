name: lobster-chat-ci

on:
  push:
    paths:
      - ".github/workflows/ci.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "apps/**"
      - "crates/**"
      - "docs/**"
      - "scripts/**"
      - "README.md"
  pull_request:
    paths:
      - ".github/workflows/ci.yml"
      - "Cargo.toml"
      - "Cargo.lock"
      - "apps/**"
      - "crates/**"
      - "docs/**"
      - "scripts/**"
      - "README.md"
  workflow_dispatch:

jobs:
  rust-smoke:
    runs-on: ubuntu-latest
    env:
      TERM: xterm-256color
      LOBSTER_CHAT_ROOT: ${{ github.workspace }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable

      - name: Cache cargo registry and target
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/bin/
            ~/.cargo/registry/index/
            ~/.cargo/registry/cache/
            ~/.cargo/git/db/
            target/
          key: ${{ runner.os }}-cargo-${{ hashFiles('**/Cargo.lock') }}
          restore-keys: |
            ${{ runner.os }}-cargo-

      - name: Verify portable smoke helpers
        run: python3 scripts/test_start_terminal_unit.py

      - name: Verify release gate helper syntax
        run: |
          bash -n scripts/smoke-release-gate.sh
          bash -n scripts/smoke-public-ingress.sh
          bash -n scripts/audit-web-assets.sh

      - name: Verify release workflow contract
        run: python3 scripts/test_release_workflow_unit.py

      - name: Cargo check
        run: cargo check --workspace --quiet

      - name: Rust format check
        run: cargo fmt --all -- --check

      - name: Rust clippy
        run: cargo clippy --workspace -- -D warnings

      - name: Native Waku REST adapter clippy
        run: cargo clippy -p transport-waku --features native-waku-rest -- -D warnings

      - name: Workspace tests
        run: cargo test --workspace --quiet

      - name: Native Waku REST adapter tests
        run: cargo test -p transport-waku --features native-waku-rest --quiet

      - name: CLI smoke
        run: scripts/smoke-cli-channel.sh

      - name: Auth smoke
        run: scripts/smoke-auth-registration.sh

      - name: Resident mainline smoke
        run: scripts/smoke-resident-mainline.sh

      - name: Install layout smoke
        run: scripts/smoke-install-layout.sh

      - name: Provider federation smoke
        run: scripts/smoke-provider-federation.sh

      - name: Install Node.js for web smokes
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/lobster-web-shell/package-lock.json

      - name: Install frontend dependencies
        working-directory: apps/lobster-web-shell
        run: npm ci

      - name: Install root dependencies (playwright for repo-root smokes)
        run: npm ci --ignore-scripts

      - name: Install Playwright chromium
        working-directory: apps/lobster-web-shell
        run: npx playwright install --with-deps chromium

      - name: Web shell smoke
        run: scripts/smoke-web-shell.sh

      - name: Build gateway for web dual browser smoke
        run: cargo build --quiet -p lobster-waku-gateway

      - name: Web dual browser smoke
        env:
          SKIP_BUILD: "1"
        run: node scripts/smoke-web-dual-browser.mjs

      - name: Terminal smoke
        run: python3 scripts/test_start_terminal.py

  web-shell-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: apps/lobster-web-shell
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: npm
          cache-dependency-path: apps/lobster-web-shell/package-lock.json

      - name: Install frontend dependencies
        run: npm ci

      - name: Install Playwright chromium
        run: npx playwright install --with-deps chromium

      - name: Web shell tests
        run: npm test
